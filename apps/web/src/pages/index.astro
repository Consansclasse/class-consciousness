---
// SPDX-License-Identifier: AGPL-3.0-or-later
const title = "class-consciousness";
const matomoUrl = import.meta.env.PUBLIC_MATOMO_URL;
const matomoSiteId = import.meta.env.PUBLIC_MATOMO_SITE_ID;
---

<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    {matomoUrl && (
      <script define:vars={{ matomoUrl, matomoSiteId }}>
        var _paq = window._paq = window._paq || [];
        _paq.push(['disableCookies']);
        _paq.push(['setDoNotTrack', true]);
        _paq.push(['anonymizeIp']);
        _paq.push(['enableHeartBeatTimer', 30]);
        _paq.push(['trackPageView']);
        _paq.push(['enableLinkTracking']);
        (function () {
          var u = matomoUrl + '/';
          _paq.push(['setTrackerUrl', u + 'matomo.php']);
          _paq.push(['setSiteId', matomoSiteId]);
          var d = document;
          var g = d.createElement('script');
          var s = d.getElementsByTagName('script')[0];
          g.async = true;
          g.src = u + 'matomo.js';
          s.parentNode.insertBefore(g, s);
        })();
      </script>
    )}
  </head>
  <body>
    <main>
      <h1>{title}</h1>
      <p>Archive open-source de la théorie marxiste — phase 0, fondations.</p>
    </main>
  </body>
</html>
